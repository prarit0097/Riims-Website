# Dependencies
node_modules/

# Local tooling / editor artifacts
.code-review-graph/
.DS_Store
Thumbs.db
*.log

# Design source handoff (large binary, not code)
RIIMS Design System-handoff.zip

# Source book (large PDF; content is integrated into the site, book kept local only)
kidney kavach book-1.pdf
*.pdf
# Owner briefs dropped in the repo root (inputs, not project source — the decisions
# they lead to get written into RIIMS.md instead)
*.docx

# Source images for the home hero + banners (large originals; compressed copies
# ship in site/assets/ as banner-*.webp — re-run: node build/optimize-images.mjs)
/hometop.png
/1hometop.png
/assets/

# Admin runtime data (VPS-only; never in git)
data/leads.json
data/content.local.json
data/admin-config.json
site/assets/uploads/

# Build artifact: page list for the admin "Pages / SEO" tab (rewritten by every build)
data/pages-manifest.json
